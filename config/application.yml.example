database: &database
  db.username: "username"
  db.password: "password"
  db.host: "host"

mailer: &mailer
  mailer.host: localhost
  mailer.port: '3000'  

development: 
  <<: *database

test:
  <<: *database